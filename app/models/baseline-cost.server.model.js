'use strict';

/**
 * Module dependencies.
 */
var mongoose = require('mongoose'),
	Schema = mongoose.Schema;

/**
 * BaselineCost Schema
 */
var BaselineCostSchema = new Schema({
	// BaselineCost model fields   
	// ...
});

mongoose.model('BaselineCost', BaselineCostSchema);