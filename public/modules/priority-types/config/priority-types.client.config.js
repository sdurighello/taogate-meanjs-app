'use strict';

// Configuring the Articles module
angular.module('priority-types').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		//Menus.addMenuItem('topbar', 'Priority types', 'priority-types', 'dropdown', '/priority-types(/create)?');
		//Menus.addSubMenuItem('topbar', 'priority-types', 'List Priority types', 'priority-types');
		//Menus.addSubMenuItem('topbar', 'priority-types', 'New Priority type', 'priority-types/create');
	}
]);
