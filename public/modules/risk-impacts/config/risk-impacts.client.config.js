'use strict';

// Configuring the Articles module
angular.module('risk-impacts').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		//Menus.addMenuItem('topbar', 'Risk impacts', 'risk-impacts', 'dropdown', '/risk-impacts(/create)?');
		//Menus.addSubMenuItem('topbar', 'risk-impacts', 'List Risk impacts', 'risk-impacts');
		//Menus.addSubMenuItem('topbar', 'risk-impacts', 'New Risk impact', 'risk-impacts/create');
	}
]);
