'use strict';

// Configuring the Articles module
angular.module('risk-severity-assignments').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		//Menus.addMenuItem('topbar', 'Risk severity assignments', 'risk-severity-assignments', 'dropdown', '/risk-severity-assignments(/create)?');
		//Menus.addSubMenuItem('topbar', 'risk-severity-assignments', 'List Risk severity assignments', 'risk-severity-assignments');
		//Menus.addSubMenuItem('topbar', 'risk-severity-assignments', 'New Risk severity assignment', 'risk-severity-assignments/create');
	}
]);
