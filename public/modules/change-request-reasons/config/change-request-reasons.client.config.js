'use strict';

// Configuring the Articles module
angular.module('change-request-reasons').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		//Menus.addMenuItem('topbar', 'Change request reasons', 'change-request-reasons', 'dropdown', '/change-request-reasons(/create)?');
		//Menus.addSubMenuItem('topbar', 'change-request-reasons', 'List Change request reasons', 'change-request-reasons');
		//Menus.addSubMenuItem('topbar', 'change-request-reasons', 'New Change request reason', 'change-request-reasons/create');
	}
]);
