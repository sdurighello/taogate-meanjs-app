'use strict';

// Configuring the Articles module
angular.module('gate-outcome-scores').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		//Menus.addMenuItem('topbar', 'Gate outcome scores', 'gate-outcome-scores', 'dropdown', '/gate-outcome-scores(/create)?');
		//Menus.addSubMenuItem('topbar', 'gate-outcome-scores', 'List Gate outcome scores', 'gate-outcome-scores');
		//Menus.addSubMenuItem('topbar', 'gate-outcome-scores', 'New Gate outcome score', 'gate-outcome-scores/create');
	}
]);
