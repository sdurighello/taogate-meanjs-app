'use strict';

// Configuring the Articles module
angular.module('risk-probabilities').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		//Menus.addMenuItem('topbar', 'Risk probabilities', 'risk-probabilities', 'dropdown', '/risk-probabilities(/create)?');
		//Menus.addSubMenuItem('topbar', 'risk-probabilities', 'List Risk probabilities', 'risk-probabilities');
		//Menus.addSubMenuItem('topbar', 'risk-probabilities', 'New Risk probability', 'risk-probabilities/create');
	}
]);
